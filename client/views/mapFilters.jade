template(name="mapFilters")
  .container.map-search-wrap
    .mobile-control
      span Filters
    .map-filter-content
      .search-wrap
        .input-group
          span.input-group-addon
            i.fa.fa-filter
          input.map-search.form-control(type="text" placeholder="Search by event name")
          span.clear-search
            i.fa.fa-times-circle
      .toggles
        .filter
          h2 Filters
        .filters-wrap
          each getCheckedValues
            .filter-block.emergence
              h3 #{displayName}
                +checkControl variable=variable checkBoxes=getCheckboxList
              .form-group
                each values
                  .checkbox
                    label
                      input(class="#{../variable}" type="checkbox" value="#{name}" checked="#{state}")
                      | #{name}

template(name="checkControl")
  if showUncheckAll
    span.check.uncheck-all
  if showCheckAll
    span.check.check-all

