template(name="header")
  header.navbar
    .navbar-default.navbar-fixed-top(role="navigation")
      .container
        .navbar-header
          img.logo(src="/grid_logo.png")
          a.navbar-brand(href="{{pathFor 'splash'}}") EcoHealth GRID
        ul.nav.navbar-nav
          li
            a(href="{{pathFor 'about'}}") About
          li
            a(href="{{pathFor 'events'}}") Events
